name: Linux Production Bootstrap Canary

on:
  workflow_dispatch:
    inputs:
      target_environment:
        description: Target environment.
        required: true
        default: staging
      base_url:
        description: Public base URL.
        required: false
        default: https://classroompath-staging.duckdns.org
      diagnostic_mode:
        description: Diagnostic-only run.
        required: false
        default: 'false'
  workflow_call:
    inputs:
      target_environment:
        required: true
        type: string
      base_url:
        required: true
        type: string
      diagnostic_mode:
        required: false
        type: string
        default: 'false'
    outputs:
      canary_result:
        value: ${{ jobs.linux-production-bootstrap-canary.outputs.canary_result }}
      failure_boundary_id:
        value: ${{ jobs.linux-production-bootstrap-canary.outputs.failure_boundary_id }}
      failure_boundary_message:
        value: ${{ jobs.linux-production-bootstrap-canary.outputs.failure_boundary_message }}

permissions:
  contents: read
  actions: read

concurrency:
  group: linux-production-bootstrap-canary-${{ github.ref }}-${{ inputs.diagnostic_mode == 'true' && github.run_id || 'release' }}
  cancel-in-progress: false

jobs:
  linux-production-bootstrap-canary:
    name: Linux Production Bootstrap Canary
    runs-on: ubuntu-latest
    timeout-minutes: 35
    outputs:
      canary_result: ${{ steps.result.outputs.canary_result }}
      failure_boundary_id: ${{ steps.result.outputs.failure_boundary_id }}
      failure_boundary_message: ${{ steps.result.outputs.failure_boundary_message }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: ./.github/actions/setup-node

      - name: Resolve canary inputs
        id: inputs
        shell: bash
        run: |
          set -euo pipefail
          target_environment="${{ inputs.target_environment || github.event.inputs.target_environment || 'staging' }}"
          base_url="${{ inputs.base_url || github.event.inputs.base_url }}"
          if [ -z "$base_url" ]; then
            base_url="$(node scripts/deploy-targets.mjs get "$target_environment" publicUrl)"
          fi
          diagnostic_mode="${{ inputs.diagnostic_mode || github.event.inputs.diagnostic_mode || 'false' }}"
          echo "target_environment=$target_environment" >> "$GITHUB_OUTPUT"
          echo "base_url=${base_url%/}" >> "$GITHUB_OUTPUT"
          echo "diagnostic_mode=$diagnostic_mode" >> "$GITHUB_OUTPUT"

      - name: Install Linux Firefox canary dependencies
        shell: bash
        run: |
          set -euo pipefail
          npm ci --ignore-scripts
          node -e "await import('selenium-webdriver'); await import('selenium-webdriver/firefox.js')"

      - name: Resolve target host
        id: resolve-production
        shell: bash
        env:
          TARGET_ENVIRONMENT: ${{ steps.inputs.outputs.target_environment }}
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_PORT: ${{ secrets.DEPLOY_PORT }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
          DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
          STAGING_DEPLOY_HOST: ${{ secrets.STAGING_DEPLOY_HOST }}
          STAGING_DEPLOY_PORT: ${{ secrets.STAGING_DEPLOY_PORT }}
          STAGING_DEPLOY_USER: ${{ secrets.STAGING_DEPLOY_USER }}
          STAGING_DEPLOY_SSH_KEY: ${{ secrets.STAGING_DEPLOY_SSH_KEY }}
        run: |
          set -euo pipefail
          source scripts/lib/github-actions-remote.sh

          if [ "$TARGET_ENVIRONMENT" = "production" ]; then
            target_host="$DEPLOY_HOST"
            target_port="${DEPLOY_PORT:-22}"
            target_user="$DEPLOY_USER"
            target_key="$DEPLOY_SSH_KEY"
          elif [ "$TARGET_ENVIRONMENT" = "staging" ]; then
            target_host="$STAGING_DEPLOY_HOST"
            target_port="${STAGING_DEPLOY_PORT:-22}"
            target_user="$STAGING_DEPLOY_USER"
            target_key="$STAGING_DEPLOY_SSH_KEY"
          else
            echo "::error::Unsupported TARGET_ENVIRONMENT: $TARGET_ENVIRONMENT"
            exit 1
          fi

          github_actions_remote_require_values \
            "$TARGET_ENVIRONMENT canaries" \
            TARGET_HOST "$target_host" \
            TARGET_USER "$target_user" \
            TARGET_SSH_KEY "$target_key"

          github_actions_remote_install_ssh_key "$target_key" ~/.ssh/classroompath-linux-bootstrap-canary
          echo "TARGET_SSH_KEY_PATH=$HOME/.ssh/classroompath-linux-bootstrap-canary" >> "$GITHUB_ENV"

          github_actions_remote_write_resolved_host_outputs \
            "$target_host" \
            "$target_port" \
            "$target_user" \
            "$TARGET_ENVIRONMENT canaries" >> "$GITHUB_OUTPUT"

      - name: Read target billing mode
        id: read-billing-mode
        shell: bash
        env:
          TARGET_ENVIRONMENT: ${{ steps.inputs.outputs.target_environment }}
          DIAGNOSTIC_MODE: ${{ steps.inputs.outputs.diagnostic_mode }}
        run: |
          set -euo pipefail
          source scripts/lib/github-actions-remote.sh

          set +e
          billing_mode="$(github_actions_remote_read_env_key \
            "$TARGET_SSH_KEY_PATH" \
            "${{ steps.resolve-production.outputs.port }}" \
            "${{ steps.resolve-production.outputs.user }}" \
            "${{ steps.resolve-production.outputs.ip }}" \
            CP_BILLING_MODE)"
          read_status="$?"
          set -e

          if [ "$read_status" -ne 0 ]; then
            if [ "$TARGET_ENVIRONMENT" = "staging" ] && [ "$DIAGNOSTIC_MODE" = "true" ]; then
              echo "::warning::Could not read staging CP_BILLING_MODE over SSH; using manual_only for diagnostic-only canary."
              billing_mode="manual_only"
            else
              exit "$read_status"
            fi
          fi

          if [ -z "$billing_mode" ]; then
            billing_mode="manual_only"
          fi

          echo "billing_mode=$billing_mode" >> "$GITHUB_OUTPUT"

      - name: Read target client canary admin token
        if: steps.read-billing-mode.outputs.billing_mode == 'manual_only'
        shell: bash
        env:
          TARGET_ENVIRONMENT: ${{ steps.inputs.outputs.target_environment }}
          DIAGNOSTIC_MODE: ${{ steps.inputs.outputs.diagnostic_mode }}
          FALLBACK_CLIENT_CANARY_ADMIN_TOKEN: ${{ secrets.CP_CLIENT_CANARY_ADMIN_TOKEN }}
        run: |
          set -euo pipefail
          source scripts/lib/github-actions-remote.sh

          set +e
          client_canary_admin_token="$(github_actions_remote_read_env_key \
            "$TARGET_SSH_KEY_PATH" \
            "${{ steps.resolve-production.outputs.port }}" \
            "${{ steps.resolve-production.outputs.user }}" \
            "${{ steps.resolve-production.outputs.ip }}" \
            CP_CLIENT_CANARY_ADMIN_TOKEN)"
          read_status="$?"
          set -e

          if [ "$read_status" -ne 0 ]; then
            if [ "$TARGET_ENVIRONMENT" = "staging" ] && [ "$DIAGNOSTIC_MODE" = "true" ] && [ -n "$FALLBACK_CLIENT_CANARY_ADMIN_TOKEN" ]; then
              echo "::warning::Could not read staging CP_CLIENT_CANARY_ADMIN_TOKEN over SSH; using diagnostic fallback secret."
              client_canary_admin_token="$FALLBACK_CLIENT_CANARY_ADMIN_TOKEN"
            else
              exit "$read_status"
            fi
          fi

          if [ -z "$client_canary_admin_token" ]; then
            echo "::error::CP_CLIENT_CANARY_ADMIN_TOKEN is missing for ${{ steps.inputs.outputs.target_environment }}"
            exit 1
          fi

          echo "::add-mask::$client_canary_admin_token"
          echo "PRODUCTION_LINUX_BOOTSTRAP_CANARY_ADMIN_TOKEN=$client_canary_admin_token" >> "$GITHUB_ENV"

      - name: Provision production enrollment canary
        id: provision
        shell: bash
        env:
          PRODUCTION_LINUX_BOOTSTRAP_CANARY_URL: ${{ steps.inputs.outputs.base_url }}
          PRODUCTION_LINUX_BOOTSTRAP_CANARY_BILLING_MODE: ${{ steps.read-billing-mode.outputs.billing_mode }}
          PRODUCTION_LINUX_BOOTSTRAP_CANARY_ADMIN_TOKEN: ${{ env.PRODUCTION_LINUX_BOOTSTRAP_CANARY_ADMIN_TOKEN || secrets.CP_CLIENT_CANARY_ADMIN_TOKEN }}
        run: node scripts/create-production-linux-bootstrap-canary.mjs

      - name: Reset Linux client state
        shell: bash
        run: |
          set -euo pipefail
          sudo systemctl stop openpath-sse-listener.service openpath-update.timer openpath-update.service dnsmasq || true
          sudo rm -rf /etc/openpath /var/lib/openpath /var/log/openpath.log

      - name: Download live linux/install-openpath.sh enrollment script
        shell: bash
        env:
          CLASSROOM_ID: ${{ steps.provision.outputs.classroom_id }}
          ENROLLMENT_TOKEN: ${{ steps.provision.outputs.enrollment_token }}
          PRODUCTION_BASE_URL: ${{ steps.inputs.outputs.base_url }}
        run: |
          set -euo pipefail
          workspace="$RUNNER_TEMP/linux-production-bootstrap-canary"
          mkdir -p "$workspace"
          linux_script_url="$PRODUCTION_BASE_URL/api/enroll/$CLASSROOM_ID"
          curl -fsSL -H "Authorization: Bearer $ENROLLMENT_TOKEN" "$linux_script_url" -o "$workspace/install-openpath.sh"
          chmod +x "$workspace/install-openpath.sh"

      - name: Verify Linux AJAX auto-allow canary
        id: ajax
        shell: bash
        env:
          LINUX_AJAX_AUTO_ALLOW_CANARY_API_URL: ${{ steps.inputs.outputs.base_url }}
          LINUX_AJAX_AUTO_ALLOW_CANARY_GROUP_ID: ${{ steps.provision.outputs.group_id }}
          LINUX_AJAX_AUTO_ALLOW_CANARY_ADMIN_TOKEN: ${{ env.PRODUCTION_LINUX_BOOTSTRAP_CANARY_ADMIN_TOKEN || secrets.CP_CLIENT_CANARY_ADMIN_TOKEN }}
          EXPECTED_EXTENSION_ID: ${{ steps.provision.outputs.extension_id }}
        run: |
          set -euo pipefail

          restore_linux_bootstrap_canary_external_dns() {
            set +e
            echo "Restoring Linux runner DNS/connectivity after OpenPath canary runtime"

            if [ -n "${original_unprivileged_port_start:-}" ]; then
              sudo sysctl -w "net.ipv4.ip_unprivileged_port_start=$original_unprivileged_port_start" || true
            fi

            timeout --kill-after=5s 20s sudo systemctl stop openpath-sse-listener.service openpath-update.timer openpath-update.service dnsmasq || true
            timeout --kill-after=5s 20s sudo systemctl reset-failed dnsmasq || true

            if [ -x /usr/local/lib/openpath/uninstall.sh ]; then
              timeout --kill-after=5s 30s sudo /usr/local/lib/openpath/uninstall.sh --auto-yes || true
            elif command -v openpath >/dev/null 2>&1; then
              timeout --kill-after=5s 30s sudo openpath disable || true
            fi

            export DEBIAN_FRONTEND=noninteractive
            timeout --kill-after=5s 30s sudo apt-get purge -y openpath-dnsmasq || true

            sudo rm -f \
              /etc/systemd/system/dnsmasq.service.d/openpath-override.conf \
              /etc/systemd/system/dnsmasq.service.d/whitelist-override.conf \
              /etc/dnsmasq.d/openpath.conf \
              /etc/tmpfiles.d/openpath-dnsmasq.conf || true
            sudo rmdir /etc/systemd/system/dnsmasq.service.d 2>/dev/null || true
            sudo systemctl daemon-reload || true
            sudo systemctl reset-failed dnsmasq || true

            if command -v resolvectl >/dev/null 2>&1 && systemctl is-active --quiet systemd-resolved; then
              default_interface="$(ip route show default 2>/dev/null | awk '{print $5; exit}')"
              if [ -n "$default_interface" ]; then
                sudo resolvectl dns "$default_interface" 1.1.1.1 8.8.8.8 || true
              fi
              sudo resolvectl flush-caches || true
            else
              printf '%s\n' 'nameserver 1.1.1.1' 'nameserver 8.8.8.8' | sudo tee /etc/resolv.conf >/dev/null || true
            fi

            getent hosts raw.githubusercontent.com || getent hosts github.com || true
          }

          trap restore_linux_bootstrap_canary_external_dns EXIT

          original_unprivileged_port_start="$(sysctl -n net.ipv4.ip_unprivileged_port_start 2>/dev/null || true)"
          sudo sysctl -w net.ipv4.ip_unprivileged_port_start=0

          workspace="$RUNNER_TEMP/linux-production-bootstrap-canary"
          : > linux-install-openpath.log
          : > linux-ajax-auto-allow-canary.log
          if [ ! -x "$workspace/install-openpath.sh" ]; then
            echo "canary_result=failure" >> "$GITHUB_OUTPUT"
            echo "failure_boundary_id=linux-install-openpath" >> "$GITHUB_OUTPUT"
            echo "failure_boundary_message=Linux enrollment script was not downloaded before the AJAX canary." >> "$GITHUB_OUTPUT"
            exit 1
          fi

          set +e
          sudo bash "$workspace/install-openpath.sh" 2>&1 | tee linux-install-openpath.log
          install_status="${PIPESTATUS[0]}"
          set -e
          if [ "$install_status" -ne 0 ]; then
            echo "canary_result=failure" >> "$GITHUB_OUTPUT"
            echo "failure_boundary_id=linux-install-openpath" >> "$GITHUB_OUTPUT"
            echo "failure_boundary_message=Linux enrollment script failed before the AJAX auto-allow canary could run." >> "$GITHUB_OUTPUT"
            FAILURE_MESSAGE='Linux enrollment script failed before the AJAX auto-allow canary could run.' node -e 'const fs = require("node:fs"); const message = process.env.FAILURE_MESSAGE; fs.writeFileSync("production-linux-ajax-auto-allow-canary.json", JSON.stringify({ ok: false, error: message, failureBoundary: { id: "linux-install-openpath", message }, diagnosticPhases: ["linux-install-openpath"] }, null, 2));'
            exit "$install_status"
          fi

          set +e
          LINUX_AJAX_AUTO_ALLOW_CANARY_PORT=80 timeout --kill-after=30s 10m node scripts/linux-ajax-auto-allow-canary.mjs 2>&1 | tee linux-ajax-auto-allow-canary.log
          ajax_status="${PIPESTATUS[0]}"
          set -e
          exit "$ajax_status"

      - name: Summarize Linux AJAX auto-allow evidence
        id: ajax-summary
        if: always()
        timeout-minutes: 2
        continue-on-error: true
        shell: bash
        run: |
          node scripts/summarize-linux-ajax-auto-allow-evidence.mjs \
            --artifact production-linux-ajax-auto-allow-canary.json \
            --summary linux-ajax-auto-allow-canary-summary.md

      - name: Prepare Linux canary artifact bundle
        id: bundle
        if: ${{ always() }}
        timeout-minutes: 2
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          tar -czf linux-production-bootstrap-canary-evidence.tgz \
            production-linux-bootstrap-canary.json \
            production-linux-ajax-auto-allow-canary.json \
            linux-install-openpath.log \
            linux-ajax-auto-allow-canary.log \
            linux-ajax-auto-allow-canary-summary.md
          ls -lh linux-production-bootstrap-canary-evidence.tgz
          tar -tzf linux-production-bootstrap-canary-evidence.tgz

      - name: Emit Linux canary evidence summary
        if: ${{ always() }}
        timeout-minutes: 2
        continue-on-error: true
        shell: bash
        env:
          CANARY_RESULT: ${{ steps.ajax-summary.outputs.canary_result || steps.ajax.outcome }}
          FAILURE_BOUNDARY_ID: ${{ steps.ajax.outputs.failure_boundary_id || steps.ajax-summary.outputs.failure_boundary_id || 'artifact-written' }}
          FAILURE_BOUNDARY_MESSAGE: ${{ steps.ajax.outputs.failure_boundary_message || steps.ajax-summary.outputs.failure_boundary_message || 'Linux AJAX evidence artifact was not written.' }}
          BUNDLE_STATUS: ${{ steps.bundle.outcome }}
        run: |
          set -euo pipefail
          echo "Writing Linux canary evidence summary to $GITHUB_STEP_SUMMARY"
          bundle_size="missing"
          if [ -f linux-production-bootstrap-canary-evidence.tgz ]; then
            bundle_size="$(wc -c < linux-production-bootstrap-canary-evidence.tgz | tr -d ' ') bytes"
          fi

          node <<'NODE' > linux-canary-evidence-summary.md
          const fs = require('node:fs');
          const summary = fs.existsSync('production-linux-ajax-auto-allow-canary.json')
            ? JSON.parse(fs.readFileSync('production-linux-ajax-auto-allow-canary.json', 'utf8'))
            : {};
          const diagnostics = summary.diagnostics ?? {};
          const preflight = diagnostics.preflight ?? {};
          const postAttempt = diagnostics.postAttempt ?? {};
          const lines = [
            '## Linux Canary Evidence Summary',
            '',
            `- canary_result: ${process.env.CANARY_RESULT ?? 'unknown'}`,
            `- failure_boundary_id: ${process.env.FAILURE_BOUNDARY_ID ?? 'unknown'}`,
            `- failure_boundary_message: ${process.env.FAILURE_BOUNDARY_MESSAGE ?? ''}`,
            '',
            '### diagnosticPhases',
            JSON.stringify(summary.diagnosticPhases ?? [], null, 2),
            '',
            '### originPreflight',
            JSON.stringify(preflight.originPreflight ?? null, null, 2),
            '',
            '### whitelistSeed',
            JSON.stringify(preflight.enrollmentSeed ?? null, null, 2),
            '',
            '### dns',
            JSON.stringify({
              preflight: preflight.dns ?? null,
              postAttempt: postAttempt.dns ?? null,
            }, null, 2),
            '',
            `- bundle_status: ${process.env.BUNDLE_STATUS ?? 'unknown'}`,
            `- bundle_size: ${process.env.BUNDLE_SIZE ?? 'unknown'}`,
          ];
          console.log(lines.join('\n'));
          NODE

          BUNDLE_SIZE="$bundle_size" node <<'NODE' > linux-canary-evidence-summary.md.tmp
          const fs = require('node:fs');
          let text = fs.readFileSync('linux-canary-evidence-summary.md', 'utf8');
          text = text.replace('- bundle_size: unknown', `- bundle_size: ${process.env.BUNDLE_SIZE}`);
          process.stdout.write(text);
          NODE
          mv linux-canary-evidence-summary.md.tmp linux-canary-evidence-summary.md

          cat linux-canary-evidence-summary.md
          cat linux-canary-evidence-summary.md >> "$GITHUB_STEP_SUMMARY"

      - name: Upload production bootstrap canary artifacts
        id: upload-artifacts
        if: ${{ always() }}
        timeout-minutes: 2
        uses: actions/upload-artifact@v7
        continue-on-error: true
        with:
          name: linux-production-bootstrap-canary
          if-no-files-found: error
          path: linux-production-bootstrap-canary-evidence.tgz
          compression-level: 0
          retention-days: 14

      - name: Cleanup Linux canary runtime
        if: ${{ always() }}
        timeout-minutes: 2
        continue-on-error: true
        shell: bash
        run: |
          timeout --kill-after=5s 20s sudo systemctl stop openpath-sse-listener.service openpath-update.timer openpath-update.service dnsmasq || true
          timeout --kill-after=5s 20s pkill -TERM -f 'firefox|geckodriver|linux-ajax-auto-allow-canary.mjs' || true

      - name: Record canary result
        id: result
        if: always()
        shell: bash
        env:
          FUNCTIONAL_RESULT: ${{ steps.ajax-summary.outputs.canary_result || steps.ajax.outcome }}
          FUNCTIONAL_BOUNDARY_ID: ${{ steps.ajax.outputs.failure_boundary_id || steps.ajax-summary.outputs.failure_boundary_id || 'artifact-written' }}
          FUNCTIONAL_BOUNDARY_MESSAGE: ${{ steps.ajax.outputs.failure_boundary_message || steps.ajax-summary.outputs.failure_boundary_message || 'Linux AJAX evidence artifact was not written.' }}
          BUNDLE_OUTCOME: ${{ steps.bundle.outcome }}
          UPLOAD_OUTCOME: ${{ steps.upload-artifacts.outcome }}
        run: |
          set -euo pipefail
          canary_result="$FUNCTIONAL_RESULT"
          failure_boundary_id="$FUNCTIONAL_BOUNDARY_ID"
          failure_boundary_message="$FUNCTIONAL_BOUNDARY_MESSAGE"

          if [ "$BUNDLE_OUTCOME" = "failure" ]; then
            canary_result="failure"
            failure_boundary_id=artifact-bundle
            failure_boundary_message='Functional canary finished but the Linux evidence bundle could not be created.'
          elif [ "$UPLOAD_OUTCOME" = "failure" ]; then
            canary_result="failure"
            failure_boundary_id=artifact-upload
            failure_boundary_message='Functional canary evidence was produced but GitHub artifact upload failed.'
          elif [ "$canary_result" = "success" ]; then
            failure_boundary_id=none
            failure_boundary_message='Linux AJAX auto-allow canary completed successfully.'
          fi

          echo "canary_result=$canary_result" >> "$GITHUB_OUTPUT"
          echo "failure_boundary_id=$failure_boundary_id" >> "$GITHUB_OUTPUT"
          echo "failure_boundary_message=$failure_boundary_message" >> "$GITHUB_OUTPUT"

          if [ "$canary_result" != "success" ]; then
            echo "::error::$failure_boundary_message"
            exit 1
          fi
